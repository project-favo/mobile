import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF910029);

  // Backgrounds
  static const background = Color(0xFFECF4F7);
  static const surface = Colors.white;

  // Text
  static const textPrimary = Color(0xFF39404B);
  static const textSecondary = Color(0xFF64656D);
  static const hint = Color(0xFF8F9098);

  // Borders
  static const border = Color(0xFFC5C6CC);

  // States
  static const error = Color(0xFF910029);
  static const success = Colors.green;
  static const warning = Color(0xFFFFC107);
}
